package proebom.sorteio.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import proebom.sorteio.models.Crbm;

public interface CrbmRepository extends JpaRepository<Crbm, Long> {
  
}
